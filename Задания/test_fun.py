# p - точка которую мы находим
# n - колличество итераций
# y - бюджет( задаем сами )
# a - критерии модели, прописываем вручную
# b - критерии модели, прописываем вручную

import numpy as np

c1 = [3, 5] # n мерные вектора можно поменять цифры
c2 = [2, 9]

c1 = np.array(c1)
c2 = np.array(c2)

x = [3, 5]
y = [2, 5]

x = np.array(x)
y = np.array(y)


p = [3, 5] #вектор
n = 2
gamma = [3, 5] #гамма вектор переименовать
a = 1000 # не нашли описание
b = 1

p = np.array(p)
gamma = np.array(gamma)


def f1:
    list1 = []
    for number in range(n):
        a = (c2[n] -c1[n]) / c1[n]
        list1.append(a)
    list1 = np.array(list1)
    return result3


def check_app():
    if n + 1 / 2*b**2 * min((c2 - c1)/c1) < 2*n*((max(c1*((c2 - c1)**-1)*c2*(numpy.dot(gamma,c2))))**-1) * max(c1 / (c2 - c1)) * summ_check1() * summ_check2() - ((n+1)/n*(max(c2)*max(c1)**-2)*(max(c1/(c2-c1)))**-1 * numpy.dot(gamma,c1)
        print('проверка прошла')





#уточнить что такое i b что такое j


